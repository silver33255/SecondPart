server:
  port: 8484

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  application:
    name: notifications

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/